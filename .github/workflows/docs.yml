# Deployment is handled by Read the Docs (fujicv.readthedocs.io).
# RTD auto-builds on every push to main via the webhook it installs at import time.
# This file is kept as a placeholder — do not add gh-deploy here.
